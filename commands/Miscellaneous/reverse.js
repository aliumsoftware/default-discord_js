const { RichEmbed } = require("discord.js");
const { orange } = require("../../colors.json");

  module.exports = {
    config: {
      name: 'reverse',
      aliases: ['backwards'],
      usage: '^',
      description: ''
    }
  }